AM1.gge
        Generalized gradient expansion for APS-U AM1 dipole
gge.lte
ggeOpt.ele
        Files to optimize the parameters of the GGE model (within reason)        
ggeMatrix.ele
        Computes the transport matrix for the AM1 GGE model
ggeTraj.ele
        Computes the trajectory through the GGE model

lgbend.lte
lgbendMat.ele
        Evalute transport matrix using LGBEND model

compareMatrices
        Compare matrices from BGGEXP and LGBEND models

runSequence
        Run everything in the proper sequence

   